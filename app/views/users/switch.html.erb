<h3>ユーザー一覧</h3>
<ol>
<% @users.each do |user| %>

  <li class="user_list"><%= link_to(user.name, switch_user_path(:id => user)) %></li>

<% end %>
</ol>
<%= link_to("ログアウト", {:controller => "user", :action => "logout"}) %>
